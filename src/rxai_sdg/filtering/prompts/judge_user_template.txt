Evaluate the following conversation.

The conversation is serialized as JSON. It is either a list of interactions
(each with "query", "think", and "answer"), or an object with an optional
"system" prompt plus an "interactions" list. When a "system" prompt is present,
also judge how well the assistant follows it.

CONVERSATION_JSON:
{conversation_json}

Return only the final JSON object.
